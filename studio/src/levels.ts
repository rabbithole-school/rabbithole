/**
 * The ladder. Lives in `shared/` because the native level rail draws itself
 * from the same list before this document has even loaded.
 */
export { STUDIO_LEVELS, levelById } from "../../shared/studioLevels";
