# Turbopack requires dependency realpaths to stay under the project root. Keep
# each checkout's pnpm link graph local while pnpm shares package contents via
# its content-addressed store (APFS clones or hardlinks where supported).
node-linker=isolated
virtual-store-dir=node_modules/.pnpm
enable-global-virtual-store=false
package-import-method=auto
